第一部分是关于html的面试题总结:
一. 语义化：语义化包括html的语义化和css的语义化
     1.html的语义化是指：使用包含语义的标签恰当的表现页面内容的结构。例如h1-h6
       css的语义化是指: 为html标签添加有意义的class，方便阅读。
     2.语义化的好处（为什么需要语义化）
          在没有css样式的情况下，页面也能呈现清晰的结构
          方便浏览器，搜索引擎的解析
          方便他人对源代码的阅读
          方便团队项目的合作开发以及维护
     3.对html语义化的理解
          用正确的标签做正确的事情。
          html语义化让页面的内容结构化，结构更清晰，便于对浏览器、搜索引擎解析;
          即使在没有样式CSS情况下也以一种文档格式显示，并且是容易阅读的;
          搜索引擎的爬虫也依赖于HTML标记来确定上下文和各个关键字的权重，利于SEO;
          使阅读源代码的人对网站更容易将网站分块，便于阅读维护理解
二. html5为什么只需要写!DOCTYPE HTML?
    HTML5不基于SGML（标准通用标记语言），所以不需要对DTD（文档类型定义document type definition）进行引用，但是需要DOCTYPE来规范浏览器的行为。
三. doctype的作用？标准模式和兼容模式有什么区别？
     doctype声明位于html文档的第一行，处于html标签之前，告知浏览器的解析器用什么文档标准
     来解析这个文档。
     doctype不存在或者格式不正确会导致文档以兼容模式呈现。
     标准模式的排版和js运作模式都是以该浏览器支持的最高标准运行的。
     在兼容模式中，页面以宽松的向后兼容的方式显示，模拟老式浏览器的行为，以防止站点无法工作。
四. 行内元素，块级元素，空元素都有哪些？
     行内元素：a b span input img select
     块级元素：div p ul ol li dl dd dt h1-h6
     空元素： <br> <hr> <img> <input> <link> <meta>
五. 导入页面样式时，使用link和@import有什么区别？
    link属于XHTML标签，除了加载CSS外，还能用于定义RSS,定义rel连接属性等作用；
    而@import是CSS提供的，只能用于加载CSS
    页面被加载的时，link会同时被加载，而@import引用的CSS会等到页面被加载完再加载
    import是CSS2.1 提出的，只在IE5以上才能被识别，而link是XHTML标签，无兼容问题
六. 介绍一下对浏览器内核的认识。
    浏览器的内核分为两部分：渲染引擎（layout engineer）和js引擎
    渲染引擎：主要是负责获取网页的内容（html，xml等），整理讯息（加入css等），计算网页的显示方式，并且输入在显示器上。浏览器的内核不同对于网页的雨打解释就不同，渲染的方式也不同。
    js引擎：主要负责解释和执行js进行网页的动态效果显示。
七. 常见的浏览器内核有哪些？
     Trident内核：IE，360 ，搜狗，the world
     webkit内核：safari，chrome，（注：chrome是Blink，是webkit的分支）
     Gecko内核：Netscape6及以上     
     Persto内核：Opera7及以上（Opera原内核为Persto，现在为Blink）
八. html5有哪些新特性？移除了哪些元素？如何支持新元素？
    html5已经不是SGML的子集了，主要是关于图像，位置，存储，多任务等功能的增加。
        绘画元素：canvas
        媒介回放元素：audio和video
        存储元素：localstorage和sessionStorage
                localStorage是长期存储数据。浏览器关闭后数据不会丢失。
                sessionStorage的数据在浏览器关闭后自动删除。
        语义化更好的内容元素：article，footer，header，nav，section
        表单元素：输入类型：email，url,date pickers（date，month, week,time,
                 datetime），number，ran  ge（数值大小滑动条）
                 表单类型：datalist（输入域选项），keygen（验证用户），output（输出）
    移除的元素：纯表现样式的元素：font ，center，big ，s，tt等
               对可用性产生负面影响的元素：frame，frameset，noframe
    支持新元素：IE6，7，8通过document.createElement产生新标签
九. 如何区分html和html5？
     看在html标签之前是否有DOCTYPE，
     看代码结构是都有新增加的元素，
     元素是否有新增加的属性
十. 浏览器是怎么对html5的离线存储资源进行管理和加载呢？
    分为两种情况:一种是在线情况下，另一种是离线情况下。
       在线情况下，浏览器发现html又不有manifest（JSON文本文件中的有关app的信息）属性，他会请求
       manifest文件。如果第一次访问app，浏览器会根据manifest文件的内容下载相应的资源并进行离线
       存储。如果已经访问过app并且已经进行过离线存储了，浏览器就会使用离线的资源加载页面，然后浏
       览器会对比新的manifest文件和旧的manifest文件，如果文件没有发生变化，就不做任何操作，如果
       文件发生了变化，就重新下载文件中的资源并进行离线存储。
       离线情况下，浏览器就直接使用离线存储的资源。
十一. 描述一下cookie，localstorage，sessionstorage的区别？
           cookie是网站为了标识用户的身份而存储在本地终端的数据（通常是经过加密的），cookie的数据始终会在同源的http请求中携带（即使不需要），即会在浏览器和服务器之间来回传递。
           localstorage和sessionstorage不会自动把数据发送给服务器，仅保存在本地。
        存储大小：
            cookie的数据存储大小不能超过4K,
            localstorage和sessionstorage虽然也有大小限制，但是比cookie要大的多，可以的达到5M或者更大。
        存储期限：
            cookie：会设置一个期限，在cookie过期之气一直有效，即使浏览器关闭。
            localstorage：长期存储数据，只要不是主动删除数据，数据就不会丢失，即使关闭浏览器
            sessionstorage：在当前的浏览器窗口关闭后自动删除。
十二. lable标签的作用？
      label标签用来控制表单控件之间的关系，当用户选择该标签时，浏览器会自动将焦点转到和标签相关的表单控件，但是label标签的for属性应该与当前标签的id属性相同。
十三. iframe有什么缺点？
       <iframe src=""></iframe>,该标签是一个内联框架元素，有效的将另一个html页面嵌入到当前页面中。
       缺点：1.iframe会阻塞主页面的onload事件。
             2.iframe和主页面共享连接池，会影响页面的并行加载。
             3.搜索引擎的检索程序无法解读这种页面，不利于SEO（搜索引擎优化）
      为了避免以上的缺点，可以在js中动态的加载iframe的src属性。
十四. html的form如何关闭自动完成功能的？
       给form标签或者某个input标签设置为autocomplete=off
十五. //如何实现多个标签也之间的通信？
      利用localstoragehe sessionstorage
      用cookie
      用web worker
十六. webSocket如何兼容低浏览器？
       Adobe Flash Socket 、
       ActiveX HTMLFile (IE) 、
       基于 multipart 编码发送 XHR 、
       基于长轮询的 XHR
十七. 页面可见性（page visibility API）有哪些用途?
       通过visibilityState的值可以确定页面当前是否可见，以及计算页面打开的时间。在页面被最小化或者切换至后台应用程序时，可以暂停动画，视频和ajax轮询。
            document.visibilityState：表示下面 4 个可能状态的值
                   hidden：页面在后台标签页中或者浏览器最小化
                   visible：页面在前台标签页中
                   prerender：页面在屏幕外执行预渲染处理 document.hidden 的值为 true
                   unloaded：页面正在从内存中卸载
            所以，可以通过document.hidden可以确定页面是否被隐藏
十八. 如何咋页面上实现一个圆形的可点击区域
      map+area或者svg
      border-radius
      纯js实现 需要求一个点在不在圆上简单算法、获取鼠标坐标等等

//第一种 使用image map
<img id="blue" class="click-area" src="blue.gif" usemap="#Map" /> 
<map name="Map" id="Map" class="click-area">  <area shape="circle" coords="50,50,50"/>
</map>
#blue{
 cursor:pointer;
 width:100px;
 height:100px;
}
//第二种 使用CSS border-radius
<div id="red" class="click-area" ></div>
#red{  
 cursor:pointer;
 background:red;  
 width:100px;  
 height:100px;  
 border-radius:50%;  
} 
//第三种 使用js检测鼠标位置
<div id="yellow" class="click-area" ></div>
$("#yellow").on('click',function(e) {    
  var r = 50; 
  var x1 = $(this).offset().left+$(this).width()/2;            
  var y1 = $(this).offset().top+$(this).height()/2;   
  var x2= e.clientX;  
  var y2= e.clientY;    
  var distance = Math.abs(Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2)));    
  if (distance <= 50)  
    alert("Yes!");    
});  
十九. 实现不使用 border 画出1px高的线，在不同浏览器的标准模式与怪异模式下都能保持一致的效果
     <div style="height:1px;overflow:hidden;background:red"></div>
二十. 网页验证码时干嘛用的？为了解决什么安全问题？
      1.用于区分用户是计算机还是人，可以防止恶意破解密码，刷票，论坛灌水等等。
      2.有效的防止黑客对某一用户的特定程序暴力破解方式进行不断的登陆尝试。      



